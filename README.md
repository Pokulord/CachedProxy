# Caching Proxy Server

A lightweight HTTP caching proxy server that forwards requests to an origin server and caches responses using Redis. Built with Python and aiohttp.

## Features

- 🚀 **Asynchronous** - Built with aiohttp for high performance
- 💾 **Redis Caching** - Cache responses in Redis for fast retrieval
- 🔄 **Forward Proxy** - Forwards requests to any origin server
- 🏷️ **Cache Headers** - Adds `X-Cache: HIT/MISS` headers to responses
- 🧹 **Cache Management** - Clear cache via CLI command
- 📝 **Comprehensive Logging** - Detailed logging for debugging
- 🔒 **Header Sanitization** - Removes potentially dangerous headers

## Installation

### Prerequisites

- Python 3.8+
- Redis server (local or remote)

### Setup

1. Clone the repository:
```bash
git clone https://github.com/yourusername/caching-proxy.git
cd caching-proxy




https://roadmap.sh/projects/caching-server
